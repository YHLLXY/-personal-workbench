# 资料库模块 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将「论文资料库」升级为统一「资料库」：论文 + 视频文案笔记统一管理，支持文件夹树（多级）、标签、全文/总结、搜索，以及手动导入通道（粘贴文本 / 上传 .json/.md）。

**Architecture:** 数据层沿用 WorkbenchRepository 双实现（localStorage 本地 + Supabase 云）。**扩展 wb_papers 表而非新建**（旧数据零迁移）；新建 wb_folders 表。UI 沿 registry 注册制，`news` 模块下 `papers` 子模块改名「资料库」，路由 `/papers` 不变。

**Tech Stack:** React 19 · TypeScript · Tailwind CSS v4 · shadcn/ui · TanStack Query · Vitest · Supabase

**Spec:** `docs/superpowers/specs/2026-08-05-资料库与文案对接-design.md`（先读它）

**仓库根目录:** `E:\knowledge home\30-项目\个人工作台`（工作目录即此）

**测试命令:** `npx vitest run`（tests/ 目录）；**lint:** `npm run lint`；**build:** `npm run build`

**重要兼容约束:** ① 旧 localStorage/Supabase 数据没有新字段，读取必须补默认值；② 旧调用处 `create.mutate({...})` 不传新字段也必须编译通过（新字段在 TS 类型中全部可选）；③ 现有 32 个测试保持全绿。

---

### Task 1: 类型扩展 + 数据库迁移

**Files:**
- Modify: `src/lib/db/types.ts`
- Create: `supabase/migrations/002_add_folders.sql`

- [ ] **Step 1: 扩展 types.ts**

在 `src/lib/db/types.ts` 中：

1. 在 `Paper` 接口中**追加**以下可选字段（保持现有字段不动）：

```ts
  // v1.1 资料库扩展（可选——兼容旧数据与旧调用）
  type?: 'paper' | 'note'
  folderId?: string | null
  tags?: string[]
  content?: string | null
  summary?: string | null
  keywords?: string[]
  source?: string | null
```

2. 新增 `Folder` 接口（放在 `Paper` 之后）：

```ts
export interface Folder { id: string; name: string; parentId: string | null; sort: number }
export interface FolderInput { name: string; parentId?: string | null }
```

3. `WorkbenchRepository` 接口中，在 `deleteReview` 之后**追加**：

```ts
  listFolders(): Promise<Folder[]>
  createFolder(input: FolderInput): Promise<Folder>
  updateFolder(id: string, patch: Partial<Pick<Folder, 'name' | 'sort'>>): Promise<Folder>
  deleteFolder(id: string): Promise<void>
  moveFolder(id: string, newParentId: string | null): Promise<Folder>
```

- [ ] **Step 2: 创建迁移文件 `supabase/migrations/002_add_folders.sql`**

```sql
-- 资料库 v1.1：wb_papers 扩展 + wb_folders 新表（幂等，可重复执行）

-- wb_papers 扩展（旧数据 type 默认 'paper'，无缝兼容）
alter table public.wb_papers add column if not exists type text not null default 'paper';
alter table public.wb_papers add column if not exists folder_id text;
alter table public.wb_papers add column if not exists tags text[] not null default '{}';
alter table public.wb_papers add column if not exists content text;
alter table public.wb_papers add column if not exists summary text;
alter table public.wb_papers add column if not exists keywords text[] not null default '{}';
alter table public.wb_papers add column if not exists file_url text;
alter table public.wb_papers add column if not exists source text;

-- 文件夹树（多级：parent_id 自引用）
create table if not exists public.wb_folders (
  id text primary key,
  user_id uuid not null default auth.uid(),
  name text not null,
  parent_id text references public.wb_folders(id) on delete cascade,
  sort int not null default 0,
  created_at timestamptz not null default now()
);

alter table public.wb_folders enable row level security;
create policy p_select_wb_folders on public.wb_folders for select using (user_id = auth.uid());
create policy p_insert_wb_folders on public.wb_folders for insert with check (user_id = auth.uid());
create policy p_update_wb_folders on public.wb_folders for update using (user_id = auth.uid());
create policy p_delete_wb_folders on public.wb_folders for delete using (user_id = auth.uid());
```

- [ ] **Step 3: 验证编译**

Run: `npx tsc --noEmit`
Expected: 通过（若报 `LocalRepository`/`SupabaseRepository` 未实现新接口方法——预期，Task 2/3 补上；若报其他错误先修）。

- [ ] **Step 4: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/lib/db/types.ts supabase/migrations/002_add_folders.sql
git commit -m "feat: 资料库数据模型（Paper 扩展 + Folder 表 + 002 迁移）"
```

---

### Task 2: local-repository 扩展（TDD）

**Files:**
- Modify: `src/lib/db/local-repository.ts`
- Test: `tests/local-repository.test.ts`

- [ ] **Step 1: 写失败测试**

在 `tests/local-repository.test.ts` 的 describe 内追加：

```ts
  describe('folders', () => {
    it('创建根级与子文件夹', async () => {
      const root = await repo.createFolder({ name: '机器学习' })
      expect(root.id).toBeTruthy()
      expect(root.parentId).toBeNull()
      const sub = await repo.createFolder({ name: 'Transformer', parentId: root.id })
      expect(sub.parentId).toBe(root.id)
      expect((await repo.listFolders())).toHaveLength(2)
    })

    it('重命名与移动文件夹', async () => {
      const a = await repo.createFolder({ name: 'A' })
      const b = await repo.createFolder({ name: 'B' })
      const renamed = await repo.updateFolder(a.id, { name: 'A2' })
      expect(renamed.name).toBe('A2')
      const moved = await repo.moveFolder(b.id, a.id)
      expect(moved.parentId).toBe(a.id)
    })

    it('删除文件夹时其中资料移入未分类（不级联删除）', async () => {
      const f = await repo.createFolder({ name: '待清理' })
      const p = await repo.createPaper({ title: '论文', authors: '', arxivId: null, url: null, status: 'want', rating: null, note: null, type: 'paper', folderId: f.id, tags: [], content: null, summary: null, keywords: [], source: null })
      await repo.deleteFolder(f.id)
      expect(await repo.listFolders()).toHaveLength(0)
      const papers = await repo.listPapers()
      expect(papers.find(x => x.id === p.id)?.folderId).toBeNull()
    })
  })

  describe('papers 资料库字段', () => {
    it('创建文案笔记类型资料', async () => {
      const p = await repo.createPaper({ title: '视频文案', authors: '', arxivId: null, url: 'https://example.com/v', status: 'want', rating: null, note: null, type: 'note', folderId: null, tags: ['口播'], content: '大家好，今天聊聊。', summary: JSON.stringify({ title: 'AI 科普', key_points: ['点一'] }), keywords: ['AI'], source: 'douyin' })
      expect(p.type).toBe('note')
      expect(p.tags).toEqual(['口播'])
      expect(p.content).toBe('大家好，今天聊聊。')
    })

    it('旧数据读取自动补齐默认字段', async () => {
      // 模拟 v1.0 旧数据（无新字段）
      localStorage.setItem('wb:papers', JSON.stringify([{ id: 'old1', title: '旧论文', authors: '', arxivId: null, url: null, status: 'want', rating: null, note: null, createdAt: '2026-08-01T00:00:00.000Z' }]))
      const papers = await repo.listPapers()
      expect(papers[0].type).toBe('paper')
      expect(papers[0].folderId).toBeNull()
      expect(papers[0].tags).toEqual([])
      expect(papers[0].keywords).toEqual([])
      expect(papers[0].content).toBeNull()
    })
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/local-repository.test.ts`
Expected: 新增用例 FAIL（`repo.createFolder is not a function` / 旧数据补默认失败）。

- [ ] **Step 3: 实现 local-repository.ts**

在 `LocalRepository` 类中追加（沿用文件顶部 `read/write/insert/patch/remove` 工具函数）：

```ts
  async listFolders() { return read<Folder>('folders') }
  async createFolder(input: FolderInput) {
    return insert<Folder>('folders', { id: genId(), name: input.name, parentId: input.parentId ?? null, sort: Date.now() })
  }
  async updateFolder(id: string, p: Partial<Pick<Folder, 'name' | 'sort'>>) { return patch<Folder>('folders', id, p) }
  async deleteFolder(id: string) {
    // 防误删：资料移入未分类，子文件夹一并删除（级联子树）
    const folders = read<Folder>('folders')
    const children = new Set([id])
    let grew = true
    while (grew) {
      grew = false
      for (const f of folders) if (children.has(f.parentId ?? '') && !children.has(f.id)) { children.add(f.id); grew = true }
    }
    write('folders', folders.filter(f => !children.has(f.id)))
    // 该文件夹子树下的资料全部移到未分类
    const papers = read<Paper>('papers')
    for (const p of papers) if (p.folderId && children.has(p.folderId)) p.folderId = null
    write('papers', papers)
  }
  async moveFolder(id: string, newParentId: string | null) {
    if (newParentId === id) throw new Error('不能移动到自身')
    return patch<Folder>('folders', id, { parentId: newParentId })
  }
```

修改 `listPapers` / `createPaper` 以补默认值与透传新字段：

```ts
  async listPapers() {
    return read<Paper>('papers').map(p => ({
      type: 'paper' as const, folderId: null, tags: [], keywords: [], content: null, summary: null, source: null,
      ...p,
    }))
  }
  async createPaper(input: Omit<Paper, 'id' | 'createdAt'>) {
    return insert<Paper>('papers', { ...input, type: input.type ?? 'paper', folderId: input.folderId ?? null, tags: input.tags ?? [], keywords: input.keywords ?? [], content: input.content ?? null, summary: input.summary ?? null, source: input.source ?? null, id: genId(), createdAt: new Date().toISOString() })
  }
```

同时更新文件顶部 import：`Folder`, `FolderInput` 加入类型导入。

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/local-repository.test.ts`
Expected: 全 PASS（新旧用例）。

- [ ] **Step 5: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/lib/db/local-repository.ts tests/local-repository.test.ts
git commit -m "feat: 本地存储资料库扩展（文件夹 CRUD + Paper 新字段 + 旧数据兼容）"
```

---

### Task 3: supabase-repository 扩展（TDD）

**Files:**
- Modify: `src/lib/db/supabase-repository.ts`
- Test: `tests/supabase-repository.test.ts`

**先读** `tests/supabase-repository.test.ts` 了解现有 mock 方式（大概率 mock `supabase-client`），按同模式写新用例。

- [ ] **Step 1: 写失败测试**

在 `tests/supabase-repository.test.ts` 追加（mock 模式以文件内现有写法为准，下面给出核心断言）：

```ts
  describe('folders', () => {
    it('listFolders 映射行到 Folder（snake_case）', async () => {
      // mock sb.from('wb_folders').select() 返回 [{ id: 'f1', name: '机器学习', parent_id: null, sort: 1, created_at: '...' }]
      // 断言结果 [0] 为 { id: 'f1', name: '机器学习', parentId: null, sort: 1 }
    })

    it('createFolder 提交载荷含 name/parent_id', async () => {
      // 断言 insert 载荷：{ id: 任意, name: 'A', parent_id: null }；断言返回映射
    })
  })

  describe('papers 新字段', () => {
    it('paperFromRow 映射 type/folder_id/tags/content/summary/keywords/source', async () => {
      // mock 行含 type:'note', folder_id:'f1', tags:['口播'], content:'...', summary:'{...}', keywords:['AI'], source:'douyin'
      // 断言映射后字段正确
    })
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/supabase-repository.test.ts`
Expected: 新用例 FAIL。

- [ ] **Step 3: 实现 supabase-repository.ts**

1. `paperFromRow` 扩展（保持旧字段顺序，追加）：

```ts
function paperFromRow(r: Row): Paper {
  return {
    id: String(r.id), title: String(r.title), authors: String(r.authors),
    arxivId: r.arxiv_id as string | null, url: r.url as string | null,
    status: r.status as Paper['status'], rating: r.rating as number | null,
    note: r.note as string | null, createdAt: String(r.created_at),
    type: (r.type as Paper['type']) ?? 'paper',
    folderId: r.folder_id as string | null,
    tags: (r.tags as string[]) ?? [],
    content: r.content as string | null,
    summary: r.summary as string | null,
    keywords: (r.keywords as string[]) ?? [],
    source: r.source as string | null,
  }
}
```

2. 新增 `folderFromRow` + 四个方法（追加在类末尾、`deletePaper` 之后）：

```ts
function folderFromRow(r: Row): Folder {
  return { id: String(r.id), name: String(r.name), parentId: r.parent_id as string | null, sort: Number(r.sort ?? 0) }
}

// 类内：
  async listFolders() { const { data, error } = await this.sb.from('wb_folders').select('*').order('sort'); if (error) throw error; return (data ?? []).map(folderFromRow) }
  async createFolder(input: FolderInput) {
    const { data, error } = await this.sb.from('wb_folders').insert({ id: genId(), name: input.name, parent_id: input.parentId ?? null }).select().single()
    if (error) throw error; return folderFromRow(data)
  }
  async updateFolder(id: string, p: Partial<Pick<Folder, 'name' | 'sort'>>) {
    const { data, error } = await this.sb.from('wb_folders').update({ name: p.name, sort: p.sort }).eq('id', id).select().single()
    if (error) throw error; return folderFromRow(data)
  }
  async deleteFolder(id: string) {
    const { error: delErr } = await this.sb.from('wb_folders').delete().eq('id', id)
    if (delErr) throw delErr
    const { error: upErr } = await this.sb.from('wb_papers').update({ folder_id: null }).eq('folder_id', id)
    if (upErr) throw upErr
  }
  async moveFolder(id: string, newParentId: string | null) {
    const { data, error } = await this.sb.from('wb_folders').update({ parent_id: newParentId }).eq('id', id).select().single()
    if (error) throw error; return folderFromRow(data)
  }
```

3. `createPaper`/`updatePaper` 载荷扩展（snake_case 映射，保留 arxiv_id 旧写法）：

```ts
  async createPaper(input: Omit<Paper, 'id' | 'createdAt'>) {
    const { data, error } = await this.sb.from('wb_papers').insert({
      title: input.title, authors: input.authors, arxiv_id: input.arxivId, url: input.url,
      status: input.status, rating: input.rating, note: input.note,
      type: input.type ?? 'paper', folder_id: input.folderId ?? null,
      tags: input.tags ?? [], content: input.content ?? null,
      summary: input.summary ?? null, keywords: input.keywords ?? [], source: input.source ?? null,
      id: genId(),
    }).select().single()
    if (error) throw error; return paperFromRow(data)
  }
  async updatePaper(id: string, p: Partial<Paper>) {
    const { data, error } = await this.sb.from('wb_papers').update({
      title: p.title, authors: p.authors, arxiv_id: p.arxivId, url: p.url,
      status: p.status, rating: p.rating, note: p.note,
      type: p.type, folder_id: p.folderId, tags: p.tags, content: p.content,
      summary: p.summary, keywords: p.keywords, source: p.source,
    }).eq('id', id).select().single()
    if (error) throw error; return paperFromRow(data)
  }
```

更新文件顶部 import 加 `Folder`, `FolderInput`。

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/supabase-repository.test.ts`
Expected: 全 PASS。

- [ ] **Step 5: 全量回归**

Run: `npx vitest run`
Expected: 全部 PASS（32 旧 + 新）。

- [ ] **Step 6: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/lib/db/supabase-repository.ts tests/supabase-repository.test.ts
git commit -m "feat: Supabase 资料库扩展（文件夹 CRUD + Paper 新字段映射）"
```

---

### Task 4: 资料库 hooks（TanStack Query）

**Files:**
- Modify: `src/modules/news/api.ts`

- [ ] **Step 1: 追加 folders hooks**

在 `src/modules/news/api.ts` 末尾追加：

```ts
export const folderKeys = { all: ['folders'] as const }

export function useFolders() { return useQuery({ queryKey: folderKeys.all, queryFn: () => repository.listFolders() }) }
export function useFolderMutations() {
  const qc = useQueryClient()
  const inv = () => { qc.invalidateQueries({ queryKey: folderKeys.all }); qc.invalidateQueries({ queryKey: paperKeys.all }) }
  return {
    create: useMutation({ mutationFn: (input: { name: string; parentId?: string | null }) => repository.createFolder(input), onSuccess: inv }),
    update: useMutation({ mutationFn: ({ id, patch }: { id: string; patch: Partial<Pick<import('../../lib/db/types').Folder, 'name' | 'sort'>> }) => repository.updateFolder(id, patch), onSuccess: inv }),
    remove: useMutation({ mutationFn: (id: string) => repository.deleteFolder(id), onSuccess: inv }),
    move: useMutation({ mutationFn: ({ id, newParentId }: { id: string; newParentId: string | null }) => repository.moveFolder(id, newParentId), onSuccess: inv }),
  }
}
```

- [ ] **Step 2: 验证编译**

Run: `npx tsc --noEmit`
Expected: 通过。

- [ ] **Step 3: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/modules/news/api.ts
git commit -m "feat: 资料库 hooks（folders CRUD + 联动失效）"
```

---

### Task 5: 导入解析工具（TDD）

**Files:**
- Create: `src/lib/parse-import.ts`
- Create: `tests/parse-import.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `tests/parse-import.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { parseImportText, parseImportMarkdown } from '../src/lib/parse-import'

describe('parseImportText', () => {
  it('解析标准 JSON 导出（提取器格式）', () => {
    const json = JSON.stringify({
      version: 1, type: 'note', title: 'AI 科普', source_url: 'https://example.com/v',
      platform: 'douyin',
      summary: { title: 'AI 科普', key_points: ['点一'], quotes: ['金句'], structure: ['开篇'] },
      keywords: ['AI', '科普'], content: '大家好，今天聊聊。', segments: [],
    })
    const r = parseImportText(json)
    expect(r.title).toBe('AI 科普')
    expect(r.sourceUrl).toBe('https://example.com/v')
    expect(r.platform).toBe('douyin')
    expect(r.content).toBe('大家好，今天聊聊。')
    expect(r.keywords).toEqual(['AI', '科普'])
    expect(r.summaryJson).toContain('key_points')
  })

  it('非 JSON 文本按纯文本处理', () => {
    const r = parseImportText('大家好，这是一段文案。')
    expect(r.title).toBe('')
    expect(r.content).toBe('大家好，这是一段文案。')
    expect(r.summaryJson).toBeNull()
  })

  it('解析失败（JSON 语法错误）不抛异常，降级为纯文本', () => {
    const r = parseImportText('{"title": 坏掉的 json')
    expect(r.content.length).toBeGreaterThan(0)
    expect(r.summaryJson).toBeNull()
  })
})

describe('parseImportMarkdown', () => {
  it('解析带 frontmatter 的笔记（title/source/tags + 正文）', () => {
    const md = '---\ntitle: "视频笔记"\nsource: "https://example.com"\ntags:\n  - 视频文案\n---\n\n# 视频笔记\n\n大家好，今天聊聊。'
    const r = parseImportMarkdown(md)
    expect(r.title).toBe('视频笔记')
    expect(r.sourceUrl).toBe('https://example.com')
    expect(r.content).toContain('大家好，今天聊聊。')
  })

  it('无 frontmatter 时正文全量作为 content', () => {
    const r = parseImportMarkdown('纯正文，没有头。')
    expect(r.title).toBe('')
    expect(r.content).toBe('纯正文，没有头。')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/parse-import.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `src/lib/parse-import.ts`**

```ts
export interface ParsedImport {
  title: string
  sourceUrl: string
  platform: string
  summaryJson: string | null   // 序列化 JSON 字符串，直接存 Paper.summary
  keywords: string[]
  content: string
}

/** 尝试按 JSON 解析（提取器标准导出）；失败则当纯文本。永不抛异常。 */
export function parseImportText(text: string): ParsedImport {
  const trimmed = (text ?? '').trim()
  if (trimmed.startsWith('{')) {
    try {
      const j = JSON.parse(trimmed)
      if (j && typeof j === 'object' && typeof j.content === 'string') {
        return {
          title: typeof j.title === 'string' ? j.title : '',
          sourceUrl: typeof j.source_url === 'string' ? j.source_url : '',
          platform: typeof j.platform === 'string' ? j.platform : '',
          summaryJson: j.summary && typeof j.summary === 'object' ? JSON.stringify(j.summary) : null,
          keywords: Array.isArray(j.keywords) ? j.keywords.filter((k: unknown): k is string => typeof k === 'string') : [],
          content: j.content,
        }
      }
    } catch { /* 解析失败 → 降级纯文本 */ }
  }
  return { title: '', sourceUrl: '', platform: '', summaryJson: null, keywords: [], content: trimmed }
}

/** 解析 Markdown 笔记：提取 frontmatter 的 title/source/tags，正文作为 content。 */
export function parseImportMarkdown(text: string): ParsedImport {
  const src = text ?? ''
  const m = src.match(/^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$/)
  if (!m) return { title: '', sourceUrl: '', platform: '', summaryJson: null, keywords: [], content: src.trim() }
  const [, fm, body] = m
  let title = '', sourceUrl = '', platform = ''
  const keywords: string[] = []
  for (const line of fm.split(/\r?\n/)) {
    const t = line.match(/^title:\s*"?([^"]*?)"?\s*$/)
    if (t) title = t[1]
    const s = line.match(/^source:\s*"?([^"]*?)"?\s*$/)
    if (s) sourceUrl = s[1]
    const p = line.match(/^platform:\s*"?([^"]*?)"?\s*$/)
    if (p) platform = p[1]
    if (line.startsWith('  - ')) keywords.push(line.slice(4).trim())
  }
  return { title, sourceUrl, platform, summaryJson: null, keywords, content: body.trim() }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/parse-import.test.ts`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/lib/parse-import.ts tests/parse-import.test.ts
git commit -m "feat: 导入解析（提取器 JSON + Markdown frontmatter，容错降级）"
```

---

### Task 6: 资料库 UI（文件夹树 + 列表 + 详情抽屉 + 新建入口）

**Files:**
- Create: `src/modules/news/folder-tree.tsx`
- Create: `src/modules/news/paper-detail.tsx`
- Create: `src/modules/news/add-paper.tsx`（论文添加对话框：arXiv 搜索 / 手动 / DOI）
- Create: `src/modules/news/add-note.tsx`（文案笔记添加对话框：粘贴 / 上传文件）
- Rewrite: `src/modules/news/papers.tsx`
- Modify: `src/registry.ts`（子模块名「论文资料库」→「资料库」）

**先读这些文件作为风格参考:** `src/modules/news/notes.tsx`（表单/对话框模式）、`src/modules/news/hot.tsx`（卡片列表）、`src/components/ui/*`（现有组件）、`src/modules/overview/task-dialog.tsx`（Dialog 表单模式）。

- [ ] **Step 1: `src/modules/news/folder-tree.tsx`**

规格：
- Props: `folders: Folder[]`、`selectedId: string | null`、`onSelect: (id: string | null) => void`
- 渲染：递归组件 `TreeNode`（用 `folders.filter(f => f.parentId === parentId)` 构建子树；根级 parentId 为 null）
- 每个节点：缩进（`style={{ paddingLeft: depth * 12 }}`）、名称（点击选中，选中高亮 `bg-accent`）、hover 操作按钮（使用 `lucide-react`：`FolderPlus` 新建子文件夹、`Pencil` 重命名、`Trash2` 删除——操作用 window.prompt / confirm 即可，保持轻量；或用现有 DropdownMenu）
- 固定入口（顶部）："全部资料"（`onSelect(null)` 特殊值 `'all'`）与"未分类"（selectedId 为 `'__none__'` 时显示未分类资料）
- 折叠：节点旁 `ChevronRight`/`ChevronDown` 切换 `collapsed: Set<string>`（useState 局部状态）
- 底部：「新建文件夹」按钮（prompt 输入名字 → `create.mutate({ name })`）
- 删除确认：`confirm('删除文件夹？其中的资料将移到未分类，不删除资料本身')`；调用 `remove.mutate(id)`

- [ ] **Step 2: `src/modules/news/paper-detail.tsx`**

规格：
- Props: `paper: Paper`、`open: boolean`、`onOpenChange: (v: boolean) => void`
- 用 `Sheet`（`src/components/ui/sheet.tsx`）包裹；`SheetTitle` 放标题
- 内容区（`SheetDescription` 可放副标题 authors/createdAt）：
  - 表单字段（受控，保存按钮触发 `update.mutate({ id, patch })`）：标题 Input、作者 Input、链接 Input、状态 Select（想读/在读/读完）、评分（Select 1-5 或留空）、文件夹 Select（选项 = useFolders 树形列表 + "未分类"）、标签 Input（逗号分隔 → tags 数组）、笔记 Textarea
  - summary 展示（若 `paper.summary` 是 JSON 字符串）：解析后渲染 `**拟标题：**…`、`**核心观点：**` 列表、`**金句：**` 引用块、`**结构拆解：**` 列表——解析失败则原样 `<pre>` 显示
  - content 展示：`<pre className="whitespace-pre-wrap ...">`（若存在）
  - 删除按钮（底部，`confirm` 后 `remove.mutate(id)` + 关闭）
- 新建的文案（type='note'）也可用此抽屉编辑

- [ ] **Step 3: `src/modules/news/add-paper.tsx`**

规格：
- Dialog（`components/ui/dialog.tsx`），内部 Tabs（`components/ui/tabs.tsx`）三个 Tab：
  - **arXiv 搜索**：复用现有 `searchArxiv` + `ArxivResult`（`src/lib/arxiv.ts`）+ 现有 papers.tsx 的搜索表单逻辑（Input + Button + 结果列表 + 收藏按钮——收藏走 `create.mutate`，`arxivId` 传 `r.arxivId`）
  - **手动录入**：Form：标题*（必填）、作者、链接 URL、文件夹 Select、标签 Input → `create.mutate`
  - **DOI 直达**：Input 输 DOI（如 `10.1000/xyz`）→ 按钮"解析" → 调 `https://api.crossref.org/works/<doi>`（fetch）→ 成功自动填充标题/作者/链接（`https://doi.org/<doi>`）→ 再次"保存"入库；解析失败 toast 报错、不阻断手动填写
- 所有 create 成功 toast.success + 清空/关闭

- [ ] **Step 4: `src/modules/news/add-note.tsx`**

规格：
- Dialog，内部 Tabs 两个 Tab：
  - **粘贴文本**：Form：标题（可空，默认"未命名"）、来源链接（可空）、标签（可空，逗号分隔）、总结 Textarea（可空）、全文 Textarea（必填）
  - **上传文件**：`<input type="file" accept=".json,.md">` + FileReader 读文本 → `parseImportText`（先试 JSON，MD 用 `parseImportMarkdown`，按扩展名分发：`.json` → parseImportText，`.md` → parseImportMarkdown，其他 → toast 报错"仅支持 .json 或 .md"）→ 解析结果**预览填充**到粘贴 Tab 的对应字段并自动切到粘贴 Tab（用户可改后再保存）；解析失败 toast.error('无法解析该文件：...')
- 保存：`create.mutate({ title, authors: '', arxivId: null, url: sourceUrl || null, status: 'want', rating: null, note: null, type: 'note', folderId: 当前文件夹, tags, content, summary: summaryJson, keywords, source: platform || null })`
- 文件夹：默认取当前选中的文件夹（若在"未分类"视图则为 null）

- [ ] **Step 5: 重写 `src/modules/news/papers.tsx`**

结构（组件内组合）：
- 顶部：标题「资料库」+ 副标题"论文与视频文案，统一收纳" + 右侧筛选（状态 Select + 类型 Select：全部/论文/文案） + `+` 新建按钮（DropdownMenu：新建文件夹 / 添加论文 / 添加文案笔记）
- 布局：`flex gap-4`；左栏 `hidden md:block w-56 shrink-0`（桌面 FolderTree）；右栏 flex-1 列表
- **移动端**：左栏包进 `Sheet`（`SheetTrigger` 按钮带 `FolderTree` icon），列表全宽
- 列表项（沿用现有卡片风格 `bg-card border rounded-2xl divide-y`）：icon（`type === 'note' ? Film : FileText`）、标题（点击 → 打开详情抽屉）、authors/source 小字、状态 Select（沿用现有）、tags 徽标（Badge 组件，最多 2 个）、Trash 删除按钮
- 筛选逻辑：
  - `folderFilter`：'all' | '__none__' | folderId——'all' 显示全部；'__none__' 显示 `folderId == null`；否则 `folderId === 选中`
  - type 筛选：'all' | 'paper' | 'note'
  - 搜索：`useMemo` 过滤 title/authors/content/keywords 包含关键词（toLowerCase includes），防抖用简单 `useState` + `setTimeout` 或直接即时过滤（列表量小，即时过滤即可）
- 空状态：按当前筛选给文案（库里没资料 → "还没有资料——从导入第一篇论文或第一条文案开始"；筛选无结果 → "没有匹配的资料"）
- 加载态：Skeleton（沿用现有模式）
- 详情抽屉：`selectedPaper` state，点击条目设置并打开 Sheet
- arXiv 搜索能力由 add-paper.tsx 承载，主页面不再有搜索框

- [ ] **Step 6: 更新 registry.ts**

`src/registry.ts` 中 `{ id: 'papers', name: '论文资料库', icon: Library, path: '/papers', ... }` → `name: '资料库'`（其余不动）。

- [ ] **Step 7: 验证编译与测试**

Run:
```bash
npx tsc --noEmit
npx vitest run
npm run lint
```
Expected: 全部通过（若 lint 有无关旧警告，确认不是本次改动引入即可）。

- [ ] **Step 8: 构建**

Run: `npm run build`
Expected: 成功产出 dist/。

- [ ] **Step 9: Commit**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add src/modules/news/ src/registry.ts
git commit -m "feat: 资料库 UI（文件夹树/列表/详情抽屉/论文与文案新建入口）"
```

---

### Task 7: 最终验证

- [ ] **Step 1: 全量测试**

Run: `npx vitest run`
Expected: 全 PASS（32 旧 + 新增 folders/papers/parse-import 用例）。

- [ ] **Step 2: lint + build**

Run: `npm run lint && npm run build`
Expected: 通过。

- [ ] **Step 3: 手动验收要点回报（不启动浏览器，逐项检查代码路径）**

在完成消息中逐项确认：
1. 旧论文数据读取无崩溃（listPapers 补默认值路径）
2. 三种新建入口都存在（文件夹/论文/文案笔记）
3. 文案导入两通道（粘贴 / 上传 .json/.md）
4. 删除文件夹 → 资料移入未分类
5. 移动端文件夹树走 Sheet
6. arXiv 搜索从主页移入"添加论文"

- [ ] **Step 4: 最终 Commit（如有遗留）**

```bash
cd "E:\knowledge home\30-项目\个人工作台"
git add -u && git commit -m "chore: 资料库模块收尾"  # 仅当有未提交改动；无改动则跳过
```

**注意：所有 commit 均不 push——推送由主会话验收后统一执行。**
