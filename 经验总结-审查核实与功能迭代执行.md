# 经验总结：从代码审查报告到功能落地的完整闭环

> 日期：2026-08-28 创建；2026-08-29 增补 v1.13–v1.15 阶段（第七~九节）；2026-08-30 增补 v1.16–v1.19 阶段（第十一节）
> 项目：个人工作台（`30-项目/个人工作台`）
> 事件：一轮 72 条的代码审查报告核实与分批修复（v1.9，由执行 agent 完成），加上「打卡入口去重 + 项目详情页」功能迭代（v1.10，由复核 agent 直接执行），以及完整的 git 分模块提交、推送与部署交付。
> 本文记录跨 agent 协作中踩过的坑和验证有效的做法，供后续迭代参考。

---

## 一、代码审查报告不能照单全收

72 条"问题"逐条核实后的真实分布：**约 1/3 不属实或前提错误，5 条 CRITICAL 无一条成立**。典型误报：

- **逻辑方向看反**：「CRON_SECRET `?? ''` 静默放行」——实际是 fail-closed（缺失时全拒绝）；报告把安全设计当成了漏洞。
- **没追守卫链**：「番茄钟重复写库」——effect 里有 `remaining > 0` 守卫 + 状态切换后立即短路；「hot.tsx 无 catch」——实际有三层兜底。
- **没查数据层**：「登录别名竞态」——`phone` 是主键，数据库天然兜底。
- **没看消费方**：`sources` 参数被报成 SSRF，实际只是白名单 id 筛选。

**有效做法**：修复前先分派只读核查代理逐条引用源码验证，产出「属实/部分属实/不属实 + 真实严重度」清单，再据实出修复计划。修复计划里专门写「**明确不修清单**」并声明"发现新问题只记录不动手"，能有效阻止执行 agent 扩散改动面。

## 二、执行 agent 翻车的具体案例（已在本轮修复）

v1.9 批次（另一 agent 执行 FIX_PLAN）留了一个典型错误——**M6 把语义修反了还提交了红测试**：

- 原报告称 `tasksTotal` 统计口径错。执行 agent 没查消费方就把"累计完成数"改成"当日完成数"，但 `review.tsx:75` 的展示文案是「累计 N」，改完 UI 直接变错；同时没更新单测，**提交了一个失败的测试**（`npm test` 全绿是发布底线）。
- 正确顺序应是：改字段前先 `grep` 所有使用处 → 看 UI 文案确定语义（文案是语义的最终依据）→ 改完跑全量测试再提交。

**推论**：给执行 agent 的计划里，凡涉及"改口径/改语义"的条目，必须写明消费方文件和预期文案，不能只写"修正统计口径"这种模糊指令。

## 三、本项目沉淀的工程约定（后续迭代必须遵守）

1. **Vercel 函数单文件**：`api/*.ts` 不支持跨文件相对导入（2026-08-05 线上 FUNCTION_INVOCATION_FAILED 排障结论），新增逻辑写在同文件内，npm 包导入不受限。
2. **打卡归属单一入口**：一个习惯只在一个地方打卡。被「自我提升」行动绑定的习惯（`GrowthAction.habitId`），打卡收敛到 growth 模块；健康模块三入口（今日打卡/快速打卡/习惯管理）一律过滤（`src/modules/health/derive.ts`）。判定用"任意状态绑定"（结构归属），与行动 active/paused 无关。
3. **知识库是项目门户口的唯一事实源**：「我的项目」页只做只读镜像（10 分钟 CDN 缓存），编辑一律跳 `github.dev`，不做双写。
4. **暴露私有内容的 API 必须鉴权**：列表摘要（本就公开，历史事实）与详情全文（新增暴露面）分级处理；详情接口用 anon client `getUser(token)` 校验 JWT，`Cache-Control` 必须带 `private`。
5. **客户端体积红线**：能用服务端解决就不加前端依赖——详情页正文用 GitHub Markdown API（`POST /markdown`，官方消毒）渲染，客户端 0 新增 KB；当年为移动端首屏砍过 312KB（recharts→SVG），勿走回头路。
6. **前后端类型双份**：`api/projects.ts` 与 `src/modules/projects/api.ts` 各有一份 `ProjectInfo`，改字段必须同步，外加 `public/projects-status.json` 兜底快照。前端字段可 optional（兼容 CDN 上的旧响应），服务端 required。
7. **路由参数天然解码**：react-router 的 `useParams` 已做 URL 解码，目录名含中文时只需在发起端 `encodeURIComponent`，不要再解一次。
8. **测试环境约定**：无 supabase env → `isCloudMode=false` → 走本地仓储，可直接真实操作 localStorage；mock 网络时注意"无 token"与"鉴权失败"两条链路要分开测。

## 四、协作流程上的有效模式

- **计划文档三要素**：可执行的文件级步骤 + 每步验收标准 + 「明确不做」清单。写成执行 agent 不需要做任何决策的程度（它"比较蠢"不是理由，是约束条件）。
- **信任要分级**：机械批量修复可以外包，涉及语义/口径的修改、跨模块重构，要么自己动手，要么在计划里附上消费方证据链。本轮 v1.10 由复核者直接执行，一次通过（304 测试全绿、build/lint 干净），没有返工。
- **验证顺序**：每个模块完成立即跑该模块测试（`npx vitest run tests/xxx`）→ 全部完成后 `npm run build`（tsc 会抓出测试里 `as` 断言盖不住的类型缺失，如 `createdAt` 字段）→ `npm run lint` 并**对比改动前后警告数**（本项目基线 11 条，新增即回归）。
- **接手别人留下的红测试**：先 `git log -p` 确认是谁、在哪次提交引入，再看 UI 消费方定语义，恢复正确实现而不是迁就错误实现改测试（本次 `tasksTotal` 的处理）。

## 五、交付与发布环节的经验（v1.10 实测）

- **分模块提交**：一个逻辑变更一个 commit，按 `feat(模块)` / `fix(模块)` / `docs` 分段（本次四段：打卡去重 / 项目详情页 / tasksTotal 回滚 / changelog+文档归档）。好处：单点回滚不牵连、Vercel 部署历史里能对上每个功能。
- **提交是验证的终点而不是起点**：顺序固定为——模块测试 → `npm test` 全量 → `npm run build` → lint 警告数与基线对比（本项目 11 条）→ 全绿才 commit。任何一步红掉都先修再提，绝不让红测试进主干（v1.9 就是反面教材）。
- **接手前先看远程**：`git remote -v` + `git branch --show-current` 确认目标仓库与分支（本项目 remote 是 `YHLLXY/-personal-workbench`，注意仓库名带连字符前缀），避免推错地方。
- **git 后台维护报错要会甄别**：提交时刷屏 `could not write multi-pack-index: Permission denied` / `geometric repack failed`，但这只影响 git 后台打包维护，**不影响 commit 和 push 本身**。正确反应：用 `git log` + `git status` 确认提交真实成功、工作区干净，再决定是否处理根因（通常是杀毒软件 / OneDrive 同步 / IDE 占用 `.git` 目录）。
- **推送后要过一遍部署验证清单**：Vercel 随 push 自动部署。验证三点——①登录态点项目卡片看详情渲染与离线降级；②健康打卡三入口的过滤效果；③新增 API 的环境变量依赖要提前核对（本次详情接口复用既有 `GITHUB_TOKEN` 和 Supabase 配置，零新增，部署即用）。
- **改动语义的回滚要留痕**：修复别人引入的错误时，commit message 写明"回滚哪次提交的哪个误修、为什么"（如 `631514908`），并在代码里留一行注释说明字段语义依据（UI 文案），防止下一个执行 agent 再"修"回去。

## 七、基础设施三件套的实战教训（v1.13–v1.14）

- **AGENTS.md 值得最优先做**：把散落的制度/术语/避坑浓缩成 agent 开机自动加载的文件后，后续会话不再重复探索与犯错；文档冲突裁决顺序要在文首声明（AGENTS.md > README > VERSIONING > 经验总结）。
- **体积预算门禁要量化基线**：先实测当前值（JS 1016KB/最大 chunk 306KB）再 +10% 定阈值，写明"确需上调须在 commit 说明理由"，防止随手加依赖悄悄毁掉移动端首屏（项目曾为 -312KB 砍过 recharts）。
- **Playwright E2E 的三个首批失败全是"假设错误"而非产品 bug**：①新手引导弹层每次全新 localStorage 都会出现并拦截一切点击——用 addInitScript 预置 `wb-onboarded` 解决；②`card-enter` 入场动画导致"元素不稳定"——等 overlay 处理后自然消失；③KPI 卡在 `md:hidden` 的移动端容器里，桌面视口断言它必挂——先看清组件在哪个断点渲染。
- **E2E 断言要对齐产品语义而非通用惯例**：勾选"完成"后任务从今日列表**消失**（todayTasks 不含 done，by design），按通用惯例断言"划线保留"必错。
- **契约测试第一天生效**：有状态 fake（insert 补 default 时间戳、upsert 按冲突键合并保旧 id、delete 模拟 FK 级联、链式 `.order().order()` 稳定排序）跑同一业务脚本对比双仓储，第一天就抓到速记 `updated_at` 漂移（云端不刷新/本地不排序）。fake 的行为差异要逐条对照真实 Supabase 语义（如 supabase-js 序列化丢弃 undefined 键=部分更新）。
- **vitest 不做类型检查**：测试里的非法选项（`getAllByRole` 的 `exact`）能跑过 vitest 却过不了 tsc——把 `npm run build` 纳入发版门禁正是为这个；反过来说，**门禁报警就是真实的发布前拦截**，别绕过。

## 八、发版脚本首战的翻车与修复（v1.15 实战）

- **门禁失败必须回滚已写的版本文件**：脚本先写 changelog/package.json 再跑门禁，失败不回滚 → 下次运行读到半截版本号 1.15.0 又 +1 → **直接跳级发成 1.16.0**。修复：try/catch 内 `git checkout --` 回滚，且该缺陷要在脚本上线前用故障注入自测。
- **git push 非原子**：`git push origin main --tags` 时 main 被拒（分叉）但 **tag 会先推上去**——远程出现指向旧提交的 v1.15.0 tag。清理要分别做：`git push origin :refs/tags/vX` 删远程 tag，再推正确提交与 tag。非必要时不用 force push，用"修正提交"留痕即可。
- **`git add -A` 会把多阶段工作卷进一个 commit**——违反分模块纪律。多阶段工作用 `git add <模块路径>` 分批提交；卷错了用 `git reset --soft HEAD~1` 拆开重提。
- **Windows 双坑**：①`execSync` 走 cmd.exe，git `--pretty=%s` 的 `%` 占位符被当环境变量吞掉——用 `execFileSync('git', args)` 绕过 shell；②`import.meta.url` 的 pathname 对中文路径是 URL 编码——必须 `decodeURIComponent` 再转系统路径。
- **git 锚点匹配要容忍 CRLF**：autocrlf 检出后文件是 `
`，脚本里 `
` 结尾的字符串锚点会匹配失败——用 `?
` 正则。
- **jsdom 环境差异**：`window.confirm` 返回 falsy（不会抛错），依赖 confirm 的交互在组件测试里静默不执行——测试要 spyOn confirm。

## 九、教训的分类规律

1. **工具型失败集中在边界层**：Windows shell 语义、行尾符、URL 编码、jsdom 存根——写跨平台脚本时先列边界假设清单。
2. **测试失败优先怀疑"假设"而不是"代码"**：本轮 E2E 六连挂里五个是测试自己的假设错（弹层/动画/断点/语义/多元素），一个都没冤枉产品代码。
3. **门禁的价值在它拦截你的时候**：tsc 抓非法选项、契约测试抓双实现漂移、体积门禁抓依赖膨胀——每次被拦都是提前拦下线上事故。
4. **自动化要自测故障路径**：发版脚本只测了成功路径，失败路径留下跳级事故——上自动化之前先问"它自己失败时会怎样"。

## 十、遗留事项

- ~~v1.10 改动未提交~~ → 已按模块分 4 个 commit 推送（`d889218ac`..`3a0da9ac9`），部署后待人工验证：登录态项目详情、健康打卡过滤、快速打卡行为。
- 详情页在「未登录/断网」时的降级已做，但 `/api/projects` 列表接口仍是公开的（历史事实），如要收紧需另行立项。
- 知识库各项目文件夹的 `dir` 名与 GitHub `30-项目/` 实际目录名的一致性未逐一核对（当前 fallback 快照里 `dir = name`），若详情 404 先查这里。
- `.git` 目录偶发 `multi-pack-index: Permission denied`（后台维护失败），暂不影响使用；若 git 操作持续变慢，排查杀毒/同步盘占用。

## 十一、交互打磨与视觉工程的教训（v1.16–v1.19）

四个版本的脉络：速记多标签（1.16）→ 任务完成划线保留可撤销（1.17）→ 启动动画首版纯 CSS 手绘（1.18）→ 按反馈用 Meteocons 素材库重做 + 天气卡（1.19）。1.18 是一次**被用户打回的重做**，教训最值钱。

### 产品与交互

- **「完成后消失」不是细节是语义**：用户点完成任务找不到去哪了、误触无法撤销。成熟方案（UX StackExchange/Todoist 共识）是划线+淡化保留、勾选框即撤销入口。改法上不動旧口径（`todayTasks` 仍排除 done），新增 `todayDone` 纯函数给新分区用——新增消费方自己闭环，不做原地改语义，避免 v1.9 M6 式事故重演。
- **半吊子状态最违和**：首版 7 类天气里「阴天」只压暗+加灰云、天空还是晴朗蓝，用户一眼看出假。天气要么连天空色一起换（v1.19 的 `WEATHER_MOOD.tint` 全天染色），要么别做这个类目。
- **过渡态/垫场层也要有完整交互**：天气未就绪的 600ms 垫场天空没绑点击处理器，E2E 点得快命中的就是它，「点击跳过」静默失效。凡会出现在用户面前的状态都要可交互；且跳过的卸载要从「淡出开始」起算而不是从挂载起算，否则点击并不真正提前离场。
- **E2E 播放型动效要留测试钩子**：每个用例都要等 3 秒动画不可接受——`localStorage('wb-boot-skip')` 让常规用例跳过动画，另设两个不打钩子的专用用例验证动画本身。
- **E2E 失败先想「是不是点到了过渡态」**：截图+探针脚本（点击后打印 class）一次定位「点击其实生效了，命中的是另一个挂载阶段的元素」。

### 视觉与素材

- **该用专业素材的领域不要手画**：纯 CSS 手绘太阳/云，静态截图能过关，动起来没有生命感——用户精准批评为「背景板上嵌了块玻璃片」。零依赖不是美德，**视觉上限受实现手段限制时，换实现手段**（Meteocons 动画 SVG，MIT、零运行时、从大场景到小图标一套同源风格统一）。
- **素材获取绕墙**：`raw.githubusercontent.com` 直连被墙，但 github.com 本身可达——浅克隆仓库或 `npm pack` 拉包提取；成品入库时附 NOTICE 许可声明（MIT + 来源 + 版本），更新只从包重提取、不手改动画结构。
- **视觉交付必须亲眼看渲染结果**：`playwright-core` 批量截图 + agent 自己 Read 图审，连续抓出「主角压住品牌图标」「雷暴天太阳全亮」「雪花在亮天空上不可见」「新手引导挡住待验收卡片」五个纯代码验证发现不了的问题。配套基建：URL 预览钩子（`?boot=dusk&wx=rain` 强制场景）+ `scripts/boot-preview.mjs` 批量出图，调观感效率极高。
- **构图问题要用统一坐标系修**：主角/品牌/山的落位冲突，改 CSS 位置类一次对齐，再全量重截验证——修一张看一张容易按下葫芦浮起瓢。

### 架构与环境

- **本地验收环境必须与生产链路同构**：`/api/weather` 是 Vercel 函数，dev 环境 404 → 本地验收永远晴天，用户线上看得到的效果开发期全被掩盖。修复方式是**把归一化逻辑收敛到前端单一模块**（`src/lib/weather.ts`，动画与天气卡共用），serverless 退化成薄代理，vite `server.proxy` + `rewrite` 固定 query 直连上游——dev 与生产走同一条前端解析路径。此后再加 serverless 接口时，先想好 dev 怎么仿真。
- **小资产会被 base64 内联撑爆主包**：14 个共 51KB 的 SVG，低于 Vite 默认 4KB 阈值的被内联进 JS，最大 chunk 340→358 超预算。`build.assetsInlineLimit` 函数式排除 `.svg` 走独立文件（SW 预缓存 globPatterns 本就含 svg，离线无损）。体积门禁又一次在拦截时证明价值。
- **上游会瞬时抽风**：Open-Meteo 偶发 422/超时。非关键数据的降级链要成套：请求竞速超时（600ms）→ localStorage 缓存 → 安全默认值（晴天），React Query `retry: 1` 抗抖动，永不阻塞 UI、永不抛错到用户眼前。
- **registry `homeCard` 必须挂在有路由的子模块下**：无独立页面的卡片（天气卡）直接嵌 `home.tsx` 网格（有 `OverviewSummary` 先例），不要为挂卡而造假模块。
- **测试断言先想清语义归属**：温度取整放 parse 层（与实况口径一致）还是显示层、`heroOpacity` 断言写公式还是写魔法数——两次红灯都是断言自己想当然，代码没错。

### 沟通

- **抽象风格名词对用户无意义**（「Apple 写实风 vs Meteocons 手绘风」）——用大白话描述质感差异 + 拿成品截图对比，再附上自己的推荐和理由，让用户做选择题而不是问答题。
- **用户说「先回答我的问题，多轮讨论再开始工作」就是真要讨论**：先把方案、取舍、待拍板项摆清楚，拿到放行信号再动手；抢跑做完了也大概率被打回。
