# 版本号制度（VERSIONING）

> 生效：2026-08-28（v1.12.0 起）。依据 [SemVer 2.0](https://semver.org/)、[Keep a Changelog](https://keepachangelog.com/en/1.1.0/)、[Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) 三个业界标准，结合个人自用 PWA 的实际裁剪。
> **对执行 agent 的硬性要求：每次发布前按 §2 判定表自判版本级别并执行 §4 流程，无需用户提醒或确认**（数据破坏性变更除外，见 §2 红线）。

---

## §1 版本号的权威源与同步关系

| 位置 | 角色 | 规则 |
|---|---|---|
| `src/app/changelog.ts` | **唯一权威版本源**（用户在应用内可见） | 每次发版在数组顶部插入新条目 |
| `package.json` 的 `version` | 同步镜像（工具生态读取） | 与权威源保持一致，**发版时必须同步更新**（历史遗留：v1.12 前一直为 0.0.0，自 1.12.0 起修复对齐） |
| git tag | 发布锚点（可选但推荐） | 发版时打 `vX.Y.Z` tag 随 push 上传 |

版本号格式：`vMAJOR.MINOR.PATCH`（展示时可简写，changelog 内的 `version` 字段用完整三段，如 `'v1.12.0'`；历史条目的简写格式不强求回改）。

## §2 级别判定表（查表自判，冲突取最高）

### MAJOR —— 颠覆性改版
- 数据模型**破坏性**变更：需要用户手动迁移/清洗数据、旧备份无法导入、本地存储结构推倒重来
- 交互习惯被推翻性重设计（如导航体系从侧边栏改为完全另一套）
- 定位级变化（如从个人工具转向多人协作）
- ⚠️ 红线：涉及**破坏性数据变更的发版，必须先向用户说明并确认**，不得自判直接发
- 个人项目预期频率：极低（一年一两次以内）

### MINOR —— 新功能 / 功能增强（feat）
- 新增模块、新增页面、新增用户可感知的功能
- 现有功能显著增强（如 v1.12：学习目标加步长/速率/里程碑——能力变多了）
- 激活存量数据的新 UI（如 someday 收件箱）
- 新增外部集成（新 API 源、新推送渠道）
- 预期频率：一到两周一次或按需

### PATCH —— 修复 / 微调（fix）
- Bug 修复（功能恢复正确，不新增能力）：如 v1.11 的横幅文字颜色、番茄钟圆环
- 防御性加固：防连击、错误提示补齐、乐观更新提速（v1.11 提醒中心）
- 性能优化、安全加固、文案/样式微调、依赖小版本升级
- 判定口诀：**用户行为没有变化，只是变得正确/更快/更稳 = PATCH**
- 预期频率：随时

### 不发版
- 仅 `docs:` / `chore:` / `refactor:` / `test:` / `style:` 的提交，且无用户可感知变化：积累在仓库里，**不发版、不插 changelog**（下次发版时若已积累了值得一提的内部改进，可在 changelog 条目里附一条「内部」）

### 多改动归并规则
一次工作会话内多个 commit → **只发一个版本，级别取改动中的最高档**：
- 例：1 个 feat + 3 个 fix → 一个 MINOR（v1.12 即此情形：6 项功能激活 + 1 个修复 → minor）
- 全部是 fix → 一个 PATCH
- 追加修复刚发的版本的遗漏（发版后 24h 内发现的小问题）→ PATCH +1，changelog 追加到**该版本条目**的 items 里，不新开条目

## §3 Conventional Commits 映射（提交时的前缀决定未来的判定依据）

| 提交前缀 | 级别含义 | changelog 分类 |
|---|---|---|
| `feat:` | MINOR 信号 | 新增 |
| `fix:` | PATCH 信号 | 修复 |
| `feat!:` / `fix!:` / BREAKING CHANGE footer | MAJOR 信号 | 变更/移除 |
| `perf:` / `refactor:` | 通常不发版 | 变更 |
| `docs:` / `chore:` / `test:` / `style:` | 不发版 | — |

changelog 条目的 items 沿用现有中文风格，但每条开头用类别词锚定：`新增：…` / `修复：…` / `变更：…` / `安全：…` / `提速：…`。

## §4 发版流程（checklist，每次发布必须完整走一遍）

> 推荐用 `node scripts/release.mjs --dry-run` 预览判级与条目，确认后 `node scripts/release.mjs --yes --title "标题"` 一键完成 3-7 步（含质量门禁）。脚本只认 feat/fix/! 前缀判级，纯 docs/chore 会拒绝发版。

1. **清点改动**：`git log` 自上个版本 tag 以来的全部提交，按 §2 表归类。
2. **判级**：取最高档 → 得出新版本号。
3. **changelog**：`src/app/changelog.ts` 数组顶部插入条目（version 用完整三段 `vX.Y.Z`、date=当天、title 一句话概括、items 按类别词开头）。
4. **同步**：`package.json` 的 `version` 改为同号（去掉 v 前缀）。
5. **验证**：`npm test` 全绿 + `npm run build` 通过 + lint 警告数 ≤ 基线（当前 12）。
6. **提交**：功能改动分模块 commit 后，最后一条 `docs: bump vX.Y.Z changelog`（changelog + package.json 一起进这条）。
7. **打 tag + push**：`git tag vX.Y.Z && git push origin main --tags`。

## §5 制度回放验证（用历史版本校准判定表，证明它符合本项目实际）

| 历史版本 | 实际内容 | 按本制度应判 | 吻合 |
|---|---|---|---|
| v1.10 | 打卡去重 + 项目详情页（新页面新交互） | MINOR | ✓ |
| v1.11 | 六处修复：横幅颜色/主题/统计口径/防连击/庆祝反馈 | 混合（1 feat 庆祝体系 + 5 fix）→ MINOR | ✓（庆祝体系够 MINOR 门槛） |
| v1.12 | 六模块功能激活（全是 feat）+ 番茄钟修复 | MINOR | ✓ |
| 目标连击修复（0ba6e2e56） | 纯 fix | PATCH（若非紧跟 v1.12 发版，则应为 v1.12.1 独立发版；因 24h 内追加，并入 v1.12 不另开条目） | ✓ |

## §6 例外与说明

- **0.x 阶段**：不适用——项目已进入 1.x 稳定期。
- **v1.0.0 之前的历史**：v1.1~v1.9 的简写格式保留不改（改了反而破坏用户已有认知），从 v1.12.0 起用完整三段。
- **紧急回滚**：不回退版本号，而是发一个 PATCH 修复版本（保持只进不退）。
- 若未来引入自动化发版（semantic-release / git-cliff），本制度的判定表就是其规则配置的蓝本。
